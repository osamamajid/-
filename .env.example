# ==========================================
# متغيرات البيئة لنظام عَقيد - Aqeed
# ==========================================

# المنفذ وبيئة التشغيل
PORT=5000
NODE_ENV=development

# رابط قاعدة البيانات (SQLite للتطوير الفوري)
DATABASE_URL="file:./dev.db"

# رابط قاعدة البيانات عند استخدام PostgreSQL أو Docker Compose
# DATABASE_URL="postgresql://postgres:postgrespassword2026@localhost:5432/aqeed_db?schema=public"

# مفاتيح الأمان JWT
JWT_SECRET="aqeed_super_secret_jwt_key_2026_contracts_system_secure_random"
JWT_EXPIRES_IN="7d"

# رابط الواجهة المسموح بها في CORS
CORS_ORIGIN="http://localhost:5173"
